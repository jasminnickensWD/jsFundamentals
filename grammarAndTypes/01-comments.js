// A single line comment
// using // will comment out a single line, making this useful for notes
// We can use Ctrl + / (Windows) as a shortcut to make a comment

/* Multi-Line Comments */

/*
Using a / and an *
will create
a new multi-line
comment*/

// In VSCode, our shortcut to make a comment will also work within HTML and CSS in the same way, 
// though the syntax will be slightly different

//Comments are ignored when running code, anything inside of them will not be run, whether it is
// valid code or otherwise this makes commenting code very useful when debugging

// let myVar = 15;
