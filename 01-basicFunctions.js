/*
FUNCTIONS

Functions are processes that we call upon to run an action. They can
do the following:
    -Take in an input to process, modify, or respond to (not
        required)
    -Return a value (not required)
    -Can be involved (or called) as often as needed
*/

//(1)   (2)
function hi() {
    //(3)
    // 1: function keyword - we use this to define a function
    // 2: name of the function, followed by parentheses - this is howw we will call the function
    // 3: code goed here - this will be the "brains: of the function
}